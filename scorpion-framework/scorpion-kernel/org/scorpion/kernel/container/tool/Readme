当前版本：ASM 5.0.3

因tryCache块在运行时捕获到的异常对象由虚拟机自动压栈，并不需要程序执行入栈指令，所以堆栈跟踪功能针对在handler标签后的第一个ASTORE指令分析时会引起EmptyStackException错误，为处理此错误对源代码做如下修改：
com.css.sword.kernel.platform.component.utils.org.objectweb.asm.commons.LocalVariablesSorter中增加如下方法：
	public void visitExceptionVarInsn(final ClassWriterUtil cwu, final int opcode, final int var) {
		cwu.isExceptionAStore = true;
		visitVarInsn(opcode, var);
	}

调整异常捕获指令的handler标签后的第一个ASTORE指令调用新的visitExceptionVarInsn方法，不在调用标准的visitVarInsn方法。